{
  "name": "@f/load-image",
  "description": "Load an image in the browser to ensure it is available to be rendered immediately",
  "repository": "git://github.com/micro-js/load-image.git",
  "version": "1.0.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {},
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
